name: camera_plus
description: an addition to camera

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+4
  # ^0.9.4+3
  video_player: ^2.2.6
  flutter_countdown_timer: 4.1.0
  share_plus: ^7.1.0
  gallery_saver: ^2.3.2 #use
  saver_gallery: ^2.0.1
  fluttertoast: ^8.2.2
  font_awesome_flutter: ^10.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
